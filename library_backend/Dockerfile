FROM python:3.11-slim

# Set environment variables
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

# Set working directory
WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y \
    libpq-dev \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# Install Python dependencies
COPY requirements.txt .
RUN pip install --upgrade pip && pip install -r requirements.txt

# Copy project
COPY . .

EXPOSE 8000

# Start: run migrations, collect static, then gunicorn
CMD ["sh", "-c", "python manage.py makemigrations users books transactions && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn library_backend.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 3"]
